spring.application.name=api

spring.datasource.url=jdbc:postgresql://${DB_HOST}/vollmed-api
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}

server.error.include-stacktrace=never

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

api.seguridad.secret=${JWT_SECRET}

#Comando para Agregar Variables de entorno desde la consola (windows): $env:nombre_De_Variable = "valor de variable"
#Comando para ver todas las Variables de entorno desde la consola (windows): ls env:
#Comando para ver una variable de entorno específica desde la consola (windows): ls env:nombre_de_variable